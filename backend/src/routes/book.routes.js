import {Router} from 'express'
const router=Router

router.route("",).post()
router.route("",).post()
router.route("",).post()
router.route("",).post()
router.route("",).post()


export default router;